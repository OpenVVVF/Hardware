# Default JLCPCB ordering notes — apply to every board in this chassis.
# Per-board overrides/specs live in Boards/<Board>/fab_spec.yaml.
notes:
  - "Serial number: 2D barcode, 10x10mm — specify position when ordering."
